spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:13306/clipboard
    username: root
    password: 123456
  jpa:
    database: mysql
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8848
  tomcat:
    uri-encoding: UTF-8
